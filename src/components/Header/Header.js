import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div>
            <nav className="menu">
                <a href="#">Home </a>
                <a href="#">About </a>
                <a href="#">Team </a>
                <a href="#">All Country </a>
            </nav>
        </div>
    );
};

export default Header;